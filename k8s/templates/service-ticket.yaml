apiVersion: v1
kind: Service
metadata: 
  name: {{ .Values.ticket.name }}
  labels:
    owner: a
    
spec:
  type: LoadBalancer
  selector:
    app: {{ .Values.ticket.name }}
    ports:
      - name: {{ .Values.ticket.name }}-listener
        proticol: TCP
        port: {{ .Values.privilege.containerPort }}
        targetPort: {{ .Values.ticket.containerPort }}